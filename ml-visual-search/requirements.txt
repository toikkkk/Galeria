# GALERIA — dependencies modul modeling (ml/)
# Instal PyTorch sesuai CUDA/CPU dari https://pytorch.org bila butuh build spesifik.

torch>=2.2
torchvision>=0.17
numpy>=1.24,<2        # <2 wajib -- opencv-python<4.11 (demo webcam) belum dukung numpy 2.x
pandas>=2.0
pillow>=10.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13          # heatmap confusion matrix
pyyaml>=6.0
tqdm>=4.66

# Tahap embedding (src/embedding.py) — index katalog untuk Visual Search
faiss-cpu>=1.8

# Scraping data (notebooks/00_scrape_wikiart.ipynb) — unduh shard WikiArt
huggingface-hub>=0.24
pyarrow>=15
datasets>=2.19        # hanya untuk nama label; opsional (bisa dari dataset_infos.json)

# Hyperparameter search (notebooks/01_train.ipynb Section 3)
optuna>=3.6

# Export model (src/export.py)
onnx>=1.16
onnxruntime>=1.18
onnxscript>=0.1          # dibutuhkan torch>=2.x utk torch.onnx.export

# Demo live (notebooks/03_demo_inference.ipynb) — scan webcam
# PENTING: opencv-python (BUKAN opencv-python-headless, itu tanpa imshow/GUI)
opencv-python<4.11       # dipin -- versi lebih baru butuh numpy>=2 (lihat catatan numpy di atas)

# Opsional (belum dipakai):
# torchmetrics>=1.3    # metrik alternatif (sklearn sudah cukup)
# open-clip-torch>=2.24  # baseline CLIP zero-shot (baseline_clip di config)
